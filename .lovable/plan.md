

## Add Text Drop Shadow to Properties Hero

**`src/components/properties/PropertiesHeroBanner.tsx`**:
- Add a `style={{ textShadow: '0 2px 8px rgba(0,0,0,0.5)' }}` to the text container div (`relative z-10` div) so all hero text gets a subtle black drop shadow for extra readability against the background image.

